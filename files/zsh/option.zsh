# keybind
bindkey -d # default keybind

# history
setopt histignorealldups # 行頭スペースコマンドは記録しない
setopt hist_reduce_blanks # 余分な空白を排除して記録
setopt sharehistory # 複数の端末でリアルタイムにヒストリを共有

# completion
setopt list_types # 補完候補の表示を親切に ls -F

